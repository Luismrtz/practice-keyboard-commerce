import React from 'react'

import { CartSection, Products} from '../components';

const HomePage = () => {
    return (
        <div>
            <Products/>
            <CartSection/>
        </div>
    )
}

export default HomePage
